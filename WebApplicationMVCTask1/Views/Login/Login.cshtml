@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <link href="~/css/Stylesheet.css" rel="stylesheet" />
</head>
<body>
    <nav class="navbar navbar-expand-lg ">
        <div class="container-fluid">
            <img src="~/Images/logo.jpg" />
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0" style="margin-left:80%;">
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("SignUp","Login")">SignUp</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Login","Login")">Login</a>
                    </li>

                </ul>

            </div>
        </div>
    </nav>
    <div class="container-fluid" style="margin-left:30%;padding-right:20%;">

        @using (Html.BeginForm("CheckLogin", "Login", FormMethod.Post))
        {
        <form id="LoginForm">

            <label for="userid"><b>User ID:</b></label>
            <input type="text" id="userid" name="UserId" required>

            <label for="userid"><b>User Name:</b></label>
            <input type="text" id="userid" name="Name" required>

            <label for="password"><b>Password:</b></label>
            <input type="password" id="password" name="decryptedPassword" required>

            <button type="submit" id="btnlogin">Login</button>

            <button type="button" class="btn btn-dark" onclick="clearForm()">Clear</button>
            <br /><br />
            <p>Don't have an Account? Click on SignUp button <button type="button">SignUp</button></p>
            <br /><br />
            <p>@TempData["error"]</p>

        </form>
        }

    </div>
</body>
</html>

@*<script>
    function validateLogin() {
        debugger;
        var UserId = $("#userid").val();
        var decryptedPassword = $("#password").val();

        $.ajax({
            type: "POST",
            url: "/Login/CheckLogin",
            data: {
                UserId: UserId,
                decryptedPassword: decryptedPassword
            },
            success: function (result) {                
                console.log(result);
              
                if (result.status) {
                    alert("Login successful!");
                    window.location.href = "/Home/Index";
                } else {                   
                    if (result.message === "UserId incorrect") {
                        alert("UserId incorrect. Please check your User ID.");
                    } else if (result.message === "Password incorrect") {
                        alert("Password incorrect. Please check your password.");
                    } else {
                        var userExists = confirm("Login failed. Do you want to sign up?");

                        if (userExists) {
                            window.location.href = "/Login/SignUp";
                        }
                    }
                }
            },
            error: function (error) {
                console.error("Error:", error);
            }
        });
    }

    function clearForm() {
        $("#userid").val("");
        $("#password").val("");
    }
</script>*@


@*<script>
        function validateLogin() {
            var UserId = $("#userid").val();
            var decryptedPassword = $("#password").val();

            $.ajax({
                type: "POST",
                url: "/Login/CheckLogin",
                data: {
                    UserId: UserId,
                    decryptedPassword: decryptedPassword
                },
                success: function (result) {
                    console.log(result);

                    if (result.status) {
                        alert("Login successful!");
                    } else
                    {
                        var userExists = confirm("Login failed. Do you want to sign up?");

                        if (userExists) {
                            window.location.href = "/Login/SignUp";
                        }
                    }
                },
                error: function (error) {
                    console.error("Error:", error);
                }
            });
        }

        function clearForm() {
            $("#userid").val("");
            $("#password").val("");
        }
    </script>*@
